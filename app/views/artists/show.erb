<h2> Artist </h2>

<ul>
    <% @artist.songs.each do |song| %>
    
    <li><%= link_to song.title , song_path(song) if song.title %></li>
    
    <%end %>
    <%= pluralize(@artist.songs.count, 'song') %>
</ul>